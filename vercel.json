{
  "buildCommand": "rm -rf node_modules package-lock.json && npm install && npm run build",
  "outputDirectory": "clean/dist",
  "installCommand": "rm -rf node_modules package-lock.json && npm install"
}
